- hosts: localhost
  tasks:
    - name: Use One Workspace
      dconf:
        key: /org/gnome/desktop/wm/preferences/num-workspaces
        value: 1
    - name: Disable Dynamic Workspaces in GNOME Shell
      dconf:
        key: /org/gnome/shell/overrides/dynamic-workspaces
        value: "false"
    - name: Disable Dynamic Workspaces in Mutter
      dconf:
        key: /org/gnome/mutter/dynamic-workspaces
        value: "false"
    - name: Use 24-hour clock
      dconf:
        key: /org/gnome/desktop/interface/clock-format
        value: "'24h'"
    - name: Configure Git
      community.general.git_config:
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        scope: global
      loop:
        - { name: user.name, value: "David Strauss" }
        - { name: user.email, value: "david@davidstrauss.net" }
        - { name: init.defaultBranch, value: "main" }
        - { name: color.ui, value: "auto" }
